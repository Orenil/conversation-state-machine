{
  "name": "conversation-state-machine",
  "version": "1.0.0",
  "description": "A deterministic dialog state machine that wraps an LLM-driven agent so regulated conversations always hit required disclosures, consent, and verification steps regardless of what the model generates.",
  "type": "module",
  "main": "dist/index.js",
  "license": "MIT",
  "author": "Orenil",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "demo": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "xstate": "^5.32.5"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.23.12",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
